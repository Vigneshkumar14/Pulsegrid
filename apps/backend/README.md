# Backend Apps

Backend deployable services live here.

- `telemetry-producer`
- `telemetry-processor`

Shared Java code should not be duplicated between services. Put common contracts or reusable code under `libs/java`.
