# Frontend Placeholder

Frontend is intentionally not scaffolded yet.

When a UI is added, it should live under `apps/frontend` so the repository remains a single monorepo with:

- `apps/backend` for deployable Spring Boot services
- `apps/frontend` for the future React application
- `libs` for shared code and contracts
- `platform` for local infrastructure and deployment assets
